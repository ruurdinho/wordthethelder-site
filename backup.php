//echo $currentHour;
            //$nightHours['time'][] = $forecast->hourly->data[$i]->time;
            
        // $testNightHours = array(1, 2, 3, 4, 5, 6);
        // $array = array_diff ($testNightHours, $forecast->hourly->data->time);

        //dd($hours);

        // $i = 0;
        // foreach ($forecast->hourly->data as $hourly) {

        //     if ($i <= 7) {
        //         $i++;
        //         $nightHours = array($hourly);
        //     }
        //             dd($nightHours);
        // }


        //dd($nightHours['timestamp']);

        //dd($hours, $nightHours);

      //  dd($hours);

        // $data = $hours->map(function ($item){
        //     return get_object_vars($item);
        // });

        // Alle timestamps van nighthours in een array gooien
        // Deze array vergelijken met alle uren aan de hand van key: time
        // alles wat overeenkomt mag blijven, rest eruit gooien.

        

        //$filtered_hours = array_intersect($hours, $nightHours);


        //var_dump($filtered_hours);

        // $filtered_hours = array_filter($hours, function ($var) use ($nightHours) {
        //     // echo 'alle uren';
        //     // var_dump($var->time);

        //     echo 'alleen de nachtelijke escapades';
        //     var_dump($nightHours);
        //     //return ($var->time == $nightHours['timestamp']);
        // });


        // dd($filtered_hours); 

        //$filterBy = 'CarEnquiry'; // or Finance etc.

        //$new = array_filter($arr, function ($var) use ($filterBy) {
        //return ($var['name'] == $filterBy);
        //});

       // dd( $forecast->hourly->data[$firstNightHour]);

               //$timestamp = $forecast->daily->data[0]->time;

                       
        //$first = $forecast->hourly->data[$firstNightHour - $firstHourInJSON];
        //dd($first);